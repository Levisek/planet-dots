# V4.4 F2 — Datum scrubber + zobrazení (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zavést `simClock` jako jediného správce simulačního času (absolutní datum, play/pauza/reverse, scrub) s clampem na rozsah VSOP87, přidat datum picker + edu presety do HUD a zobrazovat reálné kalendářní datum místo `J2000 + Nd Nh`.

**Architecture:** `simClock` drží `currentDate` (Date) jako autoritu; `tick(dtReal)` ho posouvá o `dtReal × timeScale × rate`, scrub nastavuje absolutní datum (clamp −4000…+8000). `main.js` čte pozice z `positionProvider` pro `simClock.getDate()` místo dnešního `getSimulationDate(_simElapsed)`. UI (rychlostní slider z F1 + nový datum picker + presety) mluví jen se `simClock`.

**Tech Stack:** Vanilla JS ES modules, Three.js 0.184 (vendored), astronomy-engine (vendored), `node --test`. DOM UI v pixel stylu (Press Start 2P).

## Global Constraints

- **Rozsah data (cap):** rok **−4000 až +8000** (rozsah modelu VSOP87). `simClock` scrub i tick clampují `currentDate` na tento interval; na kraji UI ukáže label „rozsah modelu VSOP87".
- **Jeden zdroj času:** `simClock` je jediná autorita. Rychlostní slider (F1 `timeControls.js`), datum picker, presety i klávesy `[ ] \ 0` mluví se `simClock`. Datum label i pozice těles čtou `simClock.getDate()` — žádné druhé nezávislé mapování elapsed→datum (to byl F1 dočasný most).
- **Reálné periody (volba A z brainstormingu):** `simClock` má JEDEN uniformní rate pro všechna tělesa, poměry period zůstávají reálné. Rate volí „jak rychle plyne čas", ne per-planeta zrychlení.
- **Epocha:** J2000 = `Date.UTC(2000,0,1,12)` (shodná se `simulationDate.js`, `keplerBackend.js`, ephemeris backendem).
- **Formace jede na real-time:** formation intro (`updateFormationIntro(pool, _realElapsed, dt)`) NESMÍ být řízeno `simClock`. Scrubber je během formace zamčený (disabled), odemkne se po `done()` (formace = F3; F2 jen připraví „scrubber disabled dokud `formationActive`").
- **Reverse playback:** `timeScale < 0` → datum jde zpět (ephemeris i Kepler jsou time-symetrické). Formace/sun/wind nereversují (zachováno z F1/V4.3).
- **Offline + CSP** (ca9ec41) zachováno — žádné nové externí zdroje. Nasazení na domácí server (`nginx:alpine` nad git klonem `/opt/stacks/planet-dots/app`, `http://192.168.100.250:8093`) je statické `git pull` — F2 jen přidává soubory, žádná změna deploy postupu.
- **Commit message česky, bez footerů**, jen pod jménem uživatele. `npm test` + `npm run test:visual` musí zůstat zelené.

---

## File Structure

| Soubor | Odpovědnost | Stav |
|---|---|---|
| `src/simClock.js` | Autorita simulačního času: currentDate, play/pause/tick/scrubTo/setTimeScale, clamp, onDateChange | NEW |
| `src/simClock.test.js` | Testy clampu, ticku, reverse, listenerů | NEW |
| `src/simulationDate.js` | +`formatCalendar(date)` (reálné datum, BCE); `getSimulationDate` ponechán jako thin helper / deprecate | MODIFY |
| `src/simulationDate.test.js` | +testy formatCalendar (běžné datum, BCE, kraje) | MODIFY/NEW |
| `src/datePresets.js` | Edu presety (Apollo 11, Halleyho kometa, transit Venuše) jako `{id,label,date}` | NEW |
| `src/timeControls.js` | Rozšířit: datum picker (rok/měsíc/den) + tlačítka presetů + play/pauza; napojit vše na `simClock`; datum label = `formatCalendar` | MODIFY |
| `src/main.js` | Mainloop čte `simClock` místo `_simElapsed→getSimulationDate`; klávesy `[ ] \ 0` → simClock; formationActive gate | MODIFY |
| `index.html` + CSS | Rozšířit `#time-controls` o picker/presety, pixel styl | MODIFY |

---

## Task 1: simClock — autorita simulačního času

**Files:**
- Create: `src/simClock.js`
- Test: `src/simClock.test.js`

**Interfaces:**
- Produces:
  - `getDate() → Date`
  - `setDate(date)` / `scrubTo(date)` — nastaví absolutní datum (clamp), emituje onDateChange, `scrubTo` navíc `pause()`
  - `tick(dtRealMs)` — pokud `playing`, posune datum o `dtRealMs × timeScale × MS_PER_MS_RATE`
  - `play()` / `pause()` / `isPlaying() → bool`
  - `setTimeScale(x)` / `getTimeScale() → number` (i záporné pro reverse)
  - `onDateChange(cb) → unsubscribe`
  - Konstanty: `MIN_YEAR = -4000`, `MAX_YEAR = 8000`, epocha J2000.

- [ ] **Step 1: Napsat failing testy**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import * as clock from './simClock.js';

test('scrubTo nastaví datum a pauzuje', () => {
  clock.play();
  clock.scrubTo(new Date('1969-07-20T20:17:00Z'));
  assert.equal(clock.getDate().getUTCFullYear(), 1969);
  assert.equal(clock.isPlaying(), false);
});

test('scrubTo clampuje na [-4000, 8000]', () => {
  clock.scrubTo(new Date('0001-01-01T00:00:00Z')); // referenční rok
  const tooEarly = new Date(Date.UTC(-9000, 0, 1));
  clock.scrubTo(tooEarly);
  assert.equal(clock.getDate().getUTCFullYear(), -4000);
  const tooLate = new Date(Date.UTC(20000, 0, 1));
  clock.scrubTo(tooLate);
  assert.equal(clock.getDate().getUTCFullYear(), 8000);
});

test('tick posouvá datum jen když playing', () => {
  clock.scrubTo(new Date('2000-01-01T12:00:00Z'));
  clock.setTimeScale(1);
  clock.tick(1000); // pauznuto po scrubTo
  assert.equal(clock.getDate().toISOString(), '2000-01-01T12:00:00.000Z');
  clock.play();
  clock.tick(1000);
  assert.ok(clock.getDate().getTime() > Date.UTC(2000,0,1,12));
});

test('reverse (timeScale<0) posune datum zpět', () => {
  clock.scrubTo(new Date('2000-06-01T00:00:00Z'));
  clock.play();
  clock.setTimeScale(-1);
  clock.tick(1000);
  assert.ok(clock.getDate().getTime() < Date.UTC(2000,5,1));
});

test('onDateChange se zavolá při scrubTo', () => {
  let called = 0;
  const off = clock.onDateChange(() => { called++; });
  clock.scrubTo(new Date('2010-01-01T00:00:00Z'));
  assert.ok(called >= 1);
  off();
});
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `node --test src/simClock.test.js`
Expected: FAIL „Cannot find module './simClock.js'".

- [ ] **Step 3: Implementovat**

```js
// simClock — jediná autorita simulačního času (V4.4 F2).
// Nahrazuje F1 dočasný most getSimulationDate(_simElapsed) v main.js.

const J2000_MS = Date.UTC(2000, 0, 1, 12);
export const MIN_YEAR = -4000;
export const MAX_YEAR = 8000;

// Uniformní rate: reálné periody zachovány (volba A). 10 real-sekund = 1 rok Země
// při timeScale=1 → viditelný pohyb vnitřních planet, vnější reálně pomalé.
const DAYS_PER_SEC = 365.25 / 10;
const MS_PER_REAL_MS = (DAYS_PER_SEC * 86400 * 1000) / 1000; // sim-ms na real-ms

let _date = new Date(J2000_MS);
let _playing = true;
let _timeScale = 1;
const _listeners = [];

function clampDate(d) {
  const y = d.getUTCFullYear();
  if (y < MIN_YEAR) return new Date(Date.UTC(MIN_YEAR, 0, 1));
  if (y > MAX_YEAR) return new Date(Date.UTC(MAX_YEAR, 11, 31));
  return d;
}

function emit() { for (const cb of _listeners) cb(_date); }

export function getDate() { return _date; }
export function isPlaying() { return _playing; }
export function play() { _playing = true; }
export function pause() { _playing = false; }
export function getTimeScale() { return _timeScale; }
export function setTimeScale(x) { _timeScale = x; }

export function setDate(date) { _date = clampDate(new Date(date.getTime())); emit(); }
export function scrubTo(date) { pause(); setDate(date); }

export function tick(dtRealMs) {
  if (!_playing) return;
  const deltaSimMs = dtRealMs * _timeScale * MS_PER_REAL_MS;
  _date = clampDate(new Date(_date.getTime() + deltaSimMs));
  emit();
}

export function onDateChange(cb) {
  _listeners.push(cb);
  return () => { const i = _listeners.indexOf(cb); if (i >= 0) _listeners.splice(i, 1); };
}
```

- [ ] **Step 4: Spustit — musí projít**

Run: `node --test src/simClock.test.js`
Expected: 5 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/simClock.js src/simClock.test.js
git commit -m "simClock: autorita simulačního času (datum, play/pauza/reverse, scrub, clamp VSOP87)"
```

---

## Task 2: simulationDate — reálný kalendářní formát

**Files:**
- Modify: `src/simulationDate.js`
- Test: `src/simulationDate.test.js` (nový nebo rozšířit)

**Interfaces:**
- Consumes: nic (pure).
- Produces: `formatCalendar(date) → string` — české datum, BCE handling (rok < 1 → „N př. n. l."). `getSimulationDate`/`formatRelative` ponechány (může je ještě volat něco během migrace; smazat až po Task 4 grep-checku).

- [ ] **Step 1: Napsat failing testy**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatCalendar } from './simulationDate.js';

test('formatCalendar běžné datum', () => {
  assert.equal(formatCalendar(new Date('2026-07-27T00:00:00Z')), '27. 7. 2026');
});

test('formatCalendar rok 1 = 1 n. l.', () => {
  assert.equal(formatCalendar(new Date(Date.UTC(1, 0, 1))), '1. 1. 1');
});

test('formatCalendar BCE: astronomický rok 0 = 1 př. n. l.', () => {
  assert.equal(formatCalendar(new Date(Date.UTC(0, 0, 1))), '1. 1. 1 př. n. l.');
});

test('formatCalendar BCE: rok -44 = 45 př. n. l.', () => {
  assert.equal(formatCalendar(new Date(Date.UTC(-44, 2, 15))), '15. 3. 45 př. n. l.');
});
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `node --test src/simulationDate.test.js`
Expected: FAIL „formatCalendar is not a function".

- [ ] **Step 3: Implementovat (přidat do simulationDate.js)**

```js
// Reálné kalendářní datum (V4.4 F2). Astronomický rok 0 = 1 př. n. l.,
// rok -N = (N+1) př. n. l.
export function formatCalendar(date) {
  const y = date.getUTCFullYear();
  const d = date.getUTCDate();
  const m = date.getUTCMonth() + 1;
  if (y < 1) return `${d}. ${m}. ${1 - y} př. n. l.`;
  return `${d}. ${m}. ${y}`;
}
```

- [ ] **Step 4: Spustit — musí projít**

Run: `node --test src/simulationDate.test.js`
Expected: 4 PASS (+ existující, pokud jsou).

- [ ] **Step 5: Commit**

```bash
git add src/simulationDate.js src/simulationDate.test.js
git commit -m "simulationDate: formatCalendar — reálné české datum + BCE"
```

---

## Task 3: datePresets — edu presety

**Files:**
- Create: `src/datePresets.js`
- Test: `src/datePresets.test.js`

**Interfaces:**
- Produces: `DATE_PRESETS: Array<{ id, label, date: Date }>`.

- [ ] **Step 1: Napsat failing test**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { DATE_PRESETS } from './datePresets.js';

test('presety mají id, label, platné Date v rozsahu', () => {
  assert.ok(DATE_PRESETS.length >= 3);
  for (const p of DATE_PRESETS) {
    assert.equal(typeof p.id, 'string');
    assert.equal(typeof p.label, 'string');
    assert.ok(p.date instanceof Date && !isNaN(p.date.getTime()), p.id);
    const y = p.date.getUTCFullYear();
    assert.ok(y >= -4000 && y <= 8000, `${p.id} mimo rozsah`);
  }
});

test('obsahuje Apollo 11, Halley, transit Venuše', () => {
  const ids = DATE_PRESETS.map((p) => p.id);
  for (const id of ['apollo11', 'halley1986', 'venus-transit-2012']) {
    assert.ok(ids.includes(id), `chybí ${id}`);
  }
});
```

- [ ] **Step 2: Spustit — musí selhat**

Run: `node --test src/datePresets.test.js`
Expected: FAIL „Cannot find module".

- [ ] **Step 3: Implementovat**

```js
// Edukativní presety data pro scrubber (V4.4 F2).
export const DATE_PRESETS = [
  { id: 'apollo11', label: 'Apollo 11 (1969)', date: new Date('1969-07-20T20:17:00Z') },
  { id: 'halley1986', label: 'Halleyho kometa (1986)', date: new Date('1986-02-09T00:00:00Z') },
  { id: 'venus-transit-2012', label: 'Transit Venuše (2012)', date: new Date('2012-06-06T01:29:00Z') },
];
```

- [ ] **Step 4: Spustit — musí projít**

Run: `node --test src/datePresets.test.js`
Expected: 2 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/datePresets.js src/datePresets.test.js
git commit -m "datePresets: edu presety (Apollo 11, Halley, transit Venuše)"
```

---

## Task 4: main.js — simClock jako autorita v mainloopu

**Files:**
- Modify: `src/main.js`
- Test: `npm test` (regrese) + vizuální ověření

**Interfaces:**
- Consumes: `simClock` (Task 1), `getSimulationDate` už nepoužívat pro řízení.

- [ ] **Step 1: Nahradit F1 most simClockem**

V `main.js` mainloopu je dnes (F1 most):
```js
const dtSim = dt * getTimeScale();
_simElapsed += dtSim;
_realElapsed += dt;
const simDate = getSimulationDate(_simElapsed);
```
Nahradit:
```js
_realElapsed += dt;                 // formace/sun/wind běží dál na real-time
if (!formationActive) simClock.tick(dt * 1000);  // dt je v sekundách → ms
const simDate = simClock.getDate();
```
`formationActive` = zda běží intro (F1/F3 flag; pokud neexistuje, zatím `false` konstanta s TODO pro F3). `getTimeScale()`/`setTimeScale()` volání v main.js přesměrovat na `simClock.getTimeScale()/setTimeScale()`. `tickHyperion(_simElapsed, ...)` — Hyperion spin potřebuje monotónní čas; použít `_realElapsed` (spin nesouvisí s datem). Ověřit, že `_simElapsed` už nikde jinde není potřeba; pokud ano, odvodit z data nebo ponechat pomocnou akumulaci.

- [ ] **Step 2: Klávesy na simClock**

Najít v main.js keydown handler pro `[ ] \ 0` (dnes volají `setTimeScale` ze simMode). Přesměrovat:
- `[` / `]` — `simClock.setTimeScale(clamp(simClock.getTimeScale() ∓ krok))`
- `\` — reverse: `simClock.setTimeScale(-simClock.getTimeScale())`
- `0` — reset timeScale na 1 a `simClock.play()`
- (volitelně) `Space` už pauzuje — napojit na `simClock.play()/pause()`.

- [ ] **Step 3: Mode-change recompute přes simClock**

V `onModeChange` (main.js) dnes: `const modeChangeDate = getSimulationDate(_simElapsed); updatePlanetOrbits(..., modeChangeDate); updateMoonOrbits(modeChangeDate);` → nahradit `const d = simClock.getDate();`.

- [ ] **Step 4: Spustit testy**

Run: `npm test`
Expected: vše zelené. Pokud test importoval `getSimulationDate`/`getTimeScale` ze simMode a rozbil se, uprav ho na simClock ekvivalent.

- [ ] **Step 5: Vizuální ověření**

`npm run serve` na volném portu (netstat check, izolovaně), otevřít. Ověřit: planety se hýbou, pauza/reverse funguje, přepnutí módu nerozhodí pozice. Pak `VR_CHANNEL=chrome npm run test:visual` — zelené.

- [ ] **Step 6: Commit**

```bash
git add src/main.js
git commit -m "main.js: simClock jako autorita času (nahrazuje F1 most), klávesy + mode-change přes simClock"
```

---

## Task 5: timeControls — datum picker + presety + play/pauza

**Files:**
- Modify: `src/timeControls.js`
- Modify: `index.html` (+ CSS `#time-controls`)
- Test: manuální (DOM) + `npm run test:visual`

**Interfaces:**
- Consumes: `simClock` (Task 1), `formatCalendar` (Task 2), `DATE_PRESETS` (Task 3).

- [ ] **Step 1: Přepojit timeControls na simClock**

`timeControls.js` dnes používá `getTimeScale/setTimeScale/onTimeScaleChange` ze `simMode` a `formatRelative`. Přepojit:
- rychlostní slider + reverse tlačítko → `simClock.setTimeScale` / `simClock.getTimeScale`.
- datum label → `simClock.getDate()` přes `formatCalendar(...)`, aktualizovat v `onDateChange` (ne per-frame `requestAnimationFrame` — reaktivně na změnu data).
- `initTimeControls` už nepotřebuje `getSimElapsed` callback (datum má simClock) — odstranit parametr a jeho volání v main.js.

- [ ] **Step 2: Přidat datum picker + presety + play/pauza do UI**

Rozšířit `_container.innerHTML` o:
```html
<button id="play-pause-btn" title="Přehrát/pauza (mezerník)">⏸</button>
<input type="date" id="date-picker" />
<span id="date-label">1. 1. 2000</span>
<select id="preset-select" title="Edu presety">
  <option value="">— přejít na událost —</option>
</select>
```
Presety naplnit z `DATE_PRESETS`. Handlery:
- `date-picker` `change` → `simClock.scrubTo(new Date(value))`.
- `preset-select` `change` → najít preset dle id → `simClock.scrubTo(preset.date)`.
- `play-pause-btn` → toggle `simClock.play()/pause()`, přepnout ikonu ▶/⏸.
- `onDateChange` → aktualizovat `date-label` (`formatCalendar`) i `date-picker.value` (ISO YYYY-MM-DD; pro rok <1 picker neumí — u BCE picker disable a spolehnout se na label + presety).
- U clampu (rok = −4000/8000) přidat malý text „rozsah modelu VSOP87".

- [ ] **Step 3: Pixel styl CSS**

V `index.html` (nebo přidruženém CSS) dostylovat nové prvky Press Start 2P konzistentně s existujícím `#time-controls` (viz existující pravidla). `type="date"` nativní picker minimalisticky (font, barvy), ať nevyčnívá z pixel HUD.

- [ ] **Step 4: Ověření**

`npm test` (regrese). Manuálně v prohlížeči: picker na 1969-07-20 → planety skočí na pozice (Apollo preset ekvivalent), reálné datum v labelu, play/pauza, reverse, presety. `VR_CHANNEL=chrome npm run test:visual` zelené.

- [ ] **Step 5: Commit**

```bash
git add src/timeControls.js index.html
git commit -m "timeControls: datum picker + edu presety + play/pauza, reálné datum v HUD (simClock)"
```

---

## Self-Review (provedeno při psaní)

**Spec coverage (F2 část specu §6 F2):**
- `simClock.js` (datum, scrub, play/pause/reverse, clamp) → Task 1 ✓
- Datum picker + slider + presety + reverse UI → Task 5 ✓
- Reálné datum v HUD (`formatCalendar`) → Task 2 + Task 5 ✓
- Edu presety (Apollo 11, Halley, transit Venuše) → Task 3 ✓
- Clamp [−4000,+8000] + label → Task 1 (clamp) + Task 5 (label) ✓
- Klávesy `[ ] \ 0` → simClock → Task 4 ✓
- Jeden zdroj času (nahradit F1 most) → Task 4 ✓

**Vědomě odloženo do F3:** `formationActive` gate je ve F2 jen připraven (konstanta `false` + TODO) — plné zamčení scrubberu během formace řeší F3 (akrece). Reverse-nereversování formace/sun/wind je zachováno (běží na `_realElapsed`).

**Type consistency:** `simClock.getDate() → Date` konzumují main.js (Task 4), timeControls (Task 5); `formatCalendar(date)` (Task 2) konzumuje timeControls (Task 5); `DATE_PRESETS[].date` je Date (Task 3) → `scrubTo(Date)` (Task 1). Konzistentní.

**Rate:** `DAYS_PER_SEC = 365.25/10` v simClock je shodný se `simulationDate.js` — po migraci je simClock jediná autorita; `getSimulationDate` zůstává jen dokud Task 4 grep nepotvrdí, že ho nic nevolá, pak lze smazat (follow-up).

**Riziko:** `type="date"` HTML input neumí BCE a extrémní roky → u roků mimo běžný rozsah picker disable, scrub přes presety/label; pokrýt manuálním testem v Task 5.
